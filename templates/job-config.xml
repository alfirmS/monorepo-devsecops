<?xml version='1.0' encoding='UTF-8'?>
<!--
  ============================================================
  JOB CONFIG TEMPLATE
  Lokasi  : monorepo-devsecops/templates/job-config.xml
  Dipakai : Job Generator Pipeline untuk create/update Jenkins job
  Catatan : File ini adalah template statis — JANGAN hapus placeholder
  ============================================================

  Placeholder yang tersedia:
    {{ SERVICE }}     → nama service (e.g. payment-api)
    {{ ENV }}         → target environment (dev/uat/stag/prod)
    {{ GIT_CRED_ID }} → Jenkins credential ID untuk akses Git
    {{ SCRIPT_PATH }} → path Jenkinsfile di dalam monorepo
                        (otomatis: services/{{ SERVICE }}/Jenkinsfile)
-->
<flow-definition plugin="workflow-job@2.40">
  <description>Helm CI/CD pipeline for {{ SERVICE }} [{{ ENV }}] — auto-generated</description>
  <keepDependencies>false</keepDependencies>
  <properties>
    <jenkins.model.BuildDiscarderProperty>
      <strategy class="hudson.tasks.LogRotator">
        <daysToKeep>30</daysToKeep>
        <numToKeep>50</numToKeep>
        <artifactDaysToKeep>7</artifactDaysToKeep>
        <artifactNumToKeep>20</artifactNumToKeep>
      </strategy>
    </jenkins.model.BuildDiscarderProperty>
  </properties>
  <definition class="org.jenkinsci.plugins.workflow.cps.CpsScmFlowDefinition" plugin="workflow-cps@2.92">
    <scm class="hudson.plugins.git.GitSCM" plugin="git@4.0">
      <configVersion>2</configVersion>
      <userRemoteConfigs>
        <hudson.plugins.git.UserRemoteConfig>
          <url>git@github.com:alfirmS/monorepo-devsecops.git</url>
          <credentialsId>{{ GIT_CRED_ID }}</credentialsId>
        </hudson.plugins.git.UserRemoteConfig>
      </userRemoteConfigs>
      <branches>
        <hudson.plugins.git.BranchSpec>
          <name>*/main</name>
        </hudson.plugins.git.BranchSpec>
      </branches>
      <doGenerateSubmoduleConfigurations>false</doGenerateSubmoduleConfigurations>
      <submoduleCfg class="empty-list"/>
      <extensions/>
    </scm>
    <scriptPath>{{ SCRIPT_PATH }}</scriptPath>
    <lightweight>true</lightweight>
  </definition>
  <triggers/>
  <disabled>false</disabled>
</flow-definition>
